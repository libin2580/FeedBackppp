<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowActionBarOverlay">false</item>
        <item name="windowActionBarOverlay">false</item>

    </style>
    <style name="popupAnimation" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/animation_on</item>
        <item name="android:windowExitAnimation">@anim/animation_off</item>
    </style>
    <color name="white">#ffffff</color>

    <color name="darkblue">#10315a</color>

    <!--<style name="dialog" parent="@android:style/Theme.Dialog">-->
        <!--<item name="windowMinWidthMajor">@dimen/_80sdp</item>-->
        <!--<item name="windowMinWidthMinor">@dimen/_50sdp</item>-->
        <!--<item name="android:textSize">@dimen/_12sdp</item>-->
        <!--<item name="@android:background">@color/white</item>-->
        <!--<item name="@android:textColor">@color/darkblue</item>-->

    <!--</style>-->

    <style name="AppTheme.NoActionBar.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.NoActionBar.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />


</resources>
